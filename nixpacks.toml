[phases.setup]
nixPkgs = ['nodejs-18_x', 'npm-9_x']

[phases.install]
cmds = ['npm ci --legacy-peer-deps']

[phases.build]
cmds = ['CI=false npm run build']

[start]
cmd = 'npm start'

[phases.setup.env]
NODE_ENV = "production"
PORT = "3000" 